"""Cache backends for the granted configuration.

Under protocol v2 the cache holds a live, rotating refresh token, not just
configuration — see :mod:`fakts_next.cache.file` for the consequences.
"""
